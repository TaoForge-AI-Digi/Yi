### 杂项
-获取日期不够稳定，做一个日期工具，或者在prompt中直接注入
-聊天session自动生成摘要标题
-⚡图标上加红色角标显示待审批数
-session默认权限锁定在plan，没有和角色走，干脆取消角色默认权限防止和event冲突。？？？？？？？？？？？？？？
-归档逻辑怪怪的，event页归档，session不会消失。session删除，event会消失。
-技能前端 渲染关联任务

### 杂项 性能优化
P0 — 影响稳定性和用户体验
1. Sub-agent 级联取消 当前：abort 父 session 不会自动 cancel 子 session（sub-agent）。子 session 的 bash 进程变成孤儿进程继续跑。 方案：agent/outer.ts 的 spawnAndRunSubAgent 处，将子 session 的 AbortController 注册到父 session 的 CancelScope 上。

2. MCP 服务器共享 当前：每次 sessionLoop 都重新连接 MCP 服务器，跨 session 不共享。如果多个 session 同时跑，同一台 MCP 服务器被重复连接。 方案：MCP client 池化，按 server name 复用连接，引用计数释放。

P1 — 性能
3. DB 写批处理 当前：每个 tool 结果都单独 messageStore.addMessage + sessions.update(updated_at)。多个 tool 跑完时产生大量同步 SQLite 写入。 方案：completeRun 后批量 flush 一次，或用更高效的方式合并 updated_at 更新。

4. tool.output 客户端节流 当前：每块输出都触发 Vue reactivity + DOM 更新。快速输出的命令（如 git clone 进度条）可能产生每秒几十次更新。 方案：服务端或客户端 50ms 窗口内合并输出块再渲染。

P2 — 功能完整度
5. Shell 完整输出查看 当前：截断后只保留尾部 50KB + temp 文件路径，但 UI 上没有"查看完整输出"入口。 方案：加一个 API 端点提供 temp 文件内容，ToolDetail 截断时显示"查看完整输出"链接。

6. 事件驱动调度 当前：事件轮询间隔 10s，实时性差。 方案：事件创建时主动触发调度（已有 scheduleImmediate），减少轮询延迟。

7. 长 session 上下文压缩 当前：没有 compaction。调 keepMessages 只能手动截断。 方案：context 接近 token 上限时自动压缩历史（opencode 的 compactHistory 模式）。

P3 — 运维
8. 全局并发限制 当前：无上限。大量 session 同时跑能把 LLM provider 打到 rate limit。 方案：session-runner 加全局 max concurrency，超出返回错误或排队。

9. 监控指标 当前：无。 方案：暴露 session 数量、队列长度、bash 执行时间、DB 延迟等指标。






### 事件优化
文件: web/server/src/llm/client.ts

LLMChunk 加 type: 'usage'：

export interface LLMChunk {
  type: 'delta' | 'done' | 'error' | 'usage'
  // ... existing fields ...
  usage?: { input_tokens: number; output_tokens: number }
  usage_type?: 'stream' | 'final'  // 区分中间和最终
}
文件: web/server/src/agent/inner.ts

streamWithRetry 的 onDelta 回调里，拿到 usage chunk 时立刻 emit：

if (chunk.type === 'usage' && socket && sessionId) {
  socket.emit('usage', {
    session_id: sessionId,
    input_tokens: chunk.usage?.input_tokens || 0,
    output_tokens: chunk.usage?.output_tokens || 0,
    usage_type: 'stream',
  })
}
文件: web/client/src/stores/chat.ts

加 tokenUsage ref 和 usage socket handler：

const tokenUsage = ref({ input: 0, output: 0, total: 0 })

// 在持久 listener 区加
socket.off('usage')
socket.on('usage', (data: { session_id: string; input_tokens: number; output_tokens: number }) => {
  if (data.session_id === activeSessionId.value) {
    tokenUsage.value = {
      input: data.input_tokens,
      output: data.output_tokens,
      total: data.input_tokens + data.output_tokens,
    }
  }
})
2.2 ContextWindowRing SVG 组件
新建文件: web/client/src/components/TokenUsageRing.vue

<script setup lang="ts">
import { computed } from 'vue'

const props = defineProps<{
  used: number
  total: number
  inputTokens: number
  outputTokens: number
  reasoningTokens?: number
}>()

const pct = computed(() => Math.min(100, (props.used / props.total) * 100))
const circumference = 2 * Math.PI * 40  // r=40
const dashOffset = computed(() => circumference - (pct.value / 100) * circumference)
</script>

<template>
  <div class="context-ring" :title="`${used.toLocaleString()} / ${total.toLocaleString()} tokens`">
    <svg width="100" height="100" viewBox="0 0 100 100">
      <circle cx="50" cy="50" r="40" fill="none" stroke="#eee" stroke-width="6" />
      <circle cx="50" cy="50" r="40" fill="none" stroke="#1976d2" stroke-width="6"
        stroke-dasharray="251.2" :stroke-dashoffset="dashOffset"
        transform="rotate(-90, 50, 50)" stroke-linecap="round" />
      <text x="50" y="50" text-anchor="middle" dominant-baseline="central"
        font-size="16" font-weight="600" fill="#333">{{ Math.round(pct) }}%</text>
    </svg>
    <div class="token-bars">
      <span class="bar prompt" :style="{ width: (inputTokens / total * 100) + '%' }" />
      <span class="bar completion" :style="{ width: (outputTokens / total * 100) + '%' }" />
      <span v-if="reasoningTokens" class="bar reasoning" :style="{ width: (reasoningTokens / total * 100) + '%' }" />
    </div>
  </div>
</template>

2.3 Token 色块条
在 ConfigBar.vue 或 ChatArea.vue 中加一行：

<div v-if="tokenUsage.total > 0" class="token-bar">
  <span class="segment prompt" :style="{ flex: tokenUsage.input }" title="Prompt tokens" />
  <span class="segment completion" :style="{ flex: tokenUsage.output }" title="Completion tokens" />
  <span class="segment reasoning" v-if="tokenUsage.reasoning" :style="{ flex: tokenUsage.reasoning }" title="Reasoning tokens" />
</div>




### 前端开销优化
3.1 Reasoning tail-window
文件: web/client/src/components/ThinkingBlock.vue

加 maxReasoningChars prop，默认 3000，超出时只渲染尾部 + "… 展开"：

const props = defineProps<{
  content: string
  maxChars?: number
}>()

const displayContent = computed(() => {
  if (!props.maxChars || props.content.length <= props.maxChars) return props.content
  return `… (${props.content.length - props.maxChars} chars collapsed)\n${props.content.slice(-props.maxChars)}`
})
3.2 MessageList 增量 watch
文件: web/client/src/components/MessageList.vue

// 改为只 watch 最后一条消息的 content.length
watch(() => {
  const msgs = chatStore.activeSession?.messages
  const last = msgs?.[msgs.length - 1]
  return last ? `${last.id}:${last.content.length}:${last.is_streaming}` : ''
}, async () => {
  await nextTick()
  if (listRef.value) listRef.value.scrollTop = listRef.value.scrollHeight
})
3.3 NO_COLOR 感知
文件: web/client/src/components/Markdown/MarkdownRenderer.vue

检测 navigator.userAgent 或 window.matchMedia('prefers-reduced-motion')，在非交互环境跳过动画。